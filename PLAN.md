# 업그레이드 계획서 — 별 헤는 밤

> 상태: **검토 대기**. 확인("진행")을 주시기 전에는 코드를 고치지 않습니다.
> 작성 기준일: 2026-09-21 · 대상 커밋: `a57ee2c` · 브랜치: `claude/yun-dongju-education-app-nsuquw`

목표는 "AI가 만든 데모"를 **학생과 교사가 매일 쓰는 앱**으로 올리는 것입니다.
벤치마크는 듀오링고이되, 베끼는 대상은 화면이 아니라 **다시 열게 만드는 구조**입니다.

---

## 0. 한 장 요약

| 항목 | 지금 | 목표 |
|---|---|---|
| 첫인상 | 보라-남색 그라데이션 + 이모지 아이콘 = "AI가 만든 티" | 밤하늘·원고지 컨셉, 커스텀 SVG 아이콘, 자체 호스팅 명조 |
| 학습 구조 | 목록형 미션 3개 | 별자리 학습 경로 + 2~4분 레슨(낭독→퀴즈→감상→보상) |
| 재방문 동기 | 스트릭 표시만 | 하루 목표, 스트릭 보호권, 주간 리그, 상황별 응원 |
| 콘텐츠 | 시 19편 + 퀴즈 15개(내가 쓴 해설) | + 검증된 연대기·TMI(출처 링크 표시), 미검증 항목은 **노출 안 함** |
| 운영 안전성 | 로컬 실행 전제 | CSRF·헤더·신고/차단·계정 삭제·백업·교사 화면 |
| 배포 | 없음 | Render 배포 + 외부 cron + CI 자동 배포. 링크/홈 화면만으로 사용 |

**총 작업 규모 추정**: 현재 6,780줄 → 대략 2배. Phase 1·2가 가장 큽니다.

---

## 1. 현재 구조 (사실 정리)

```
src/
  config.js        .env 로드 + 기본값          71줄
  db.js            SQLite 스키마·마이그레이션   241줄   (테이블 16개)
  app.js server.js express 조립 / 진입점        89줄
  content/         데이터·문구 (코드 아님)     1,362줄
    poems.js         시 19편 + 해설             573줄
    missions.js      미션 템플릿 13종           311줄
    challenges.js    도전과제 23개              242줄
    quiz-bank.js     생애 문제 15개             134줄
    messages.js      봇 문구                    112줄
  services/        도메인 로직                1,143줄
  routes/          HTTP 경계                   380줄
  lib/             rng·date·korean(조사)·bus    200줄
public/            빌드 없는 PWA             1,479줄
test/              node:test, 46개             818줄
```

**지켜야 할 제약 (동의합니다)**
- 프런트엔드 빌드 단계 없음 — 지금도 없고, 끝까지 없앨 계획입니다.
- DB는 `node:sqlite` (Node 22.5+ 내장) — 네이티브 빌드 의존성 0.
- 런타임 의존성은 `express`, `web-push` 둘뿐 — **추가 계획 없음**(§6 참조).

**잘 되어 있어서 건드리지 않을 부분**
- 점수 원장(ledger) + `(user, reason, ref)` 유일 인덱스 → 중복 적립 방지. 리그·보상 확장의 토대가 됩니다.
- 퀴즈 정답을 서버에만 두고 푼 뒤에 여는 구조.
- 한국어 조사 처리기(`lib/korean.js`) — 문구가 늘어날수록 값어치가 커집니다.
- 봇 발화가 `postBotMessage()` 한 곳으로 모여 있는 점 (카카오 연동 확장 지점).

---

## 2. 발견한 문제와 리스크

심각도 순입니다. **P0은 Phase 1 시작하자마자 먼저 고칩니다.**

### P0 — 지금 당장 잘못 동작하는 것

| # | 문제 | 근거 | 영향 |
|---|---|---|---|
| 1 | **테스트가 `.env`를 읽어서 깨진다** | `.env` 있으면 46개 중 2개 실패, 치우면 46개 통과. `config.js`가 테스트에서도 `.env`를 로드하고 `AUTH_RATE_LIMIT_MAX=20`이 적용됨 | CI를 붙이는 순간 "내 컴퓨터에선 되는데" 문제가 됨. **한 줄 수정** |
| 2 | **초대 코드 무차별 대입에 제한이 없다** | `POST /api/rooms/join`에 레이트리밋 없음. 코드 6자리(32진) | 남의 반에 난입 가능 |
| 3 | **보안 헤더가 하나도 없다** | `app.js`에 CSP·nosniff·frame-options 없음 | 클릭재킹, MIME 스니핑 |

### P1 — 실사용 전에 반드시

| # | 문제 | 지금 상태 |
|---|---|---|
| 4 | 채팅 신고·차단·강제 퇴장·욕설 필터 없음 | 전무. 교실에 풀면 바로 문제가 됩니다 |
| 5 | 계정/데이터 삭제 기능 없음 | 전무. 미성년자 대상에서 특히 필요 |
| 6 | 개인정보 처리 안내 없음 | 전무 |
| 7 | 스케줄러가 프로세스 내부 타이머 | 무료 플랜에서 서버가 잠들면 06시 미션 발행이 **실행되지 않음**. `tick()`은 "정각이 아니면 건너뜀"이라 밀린 작업 따라잡기도 못 함 |
| 8 | 재배포 시 SQLite 파일 소실 가능 | 영구 디스크 설정 없음. 학급 데이터가 통째로 날아감 |
| 9 | 백업·복원 수단 없음 | 전무 |
| 10 | CSRF 방어가 SameSite=Lax 하나뿐 | 대부분 막히지만 명시적 Origin 검증이 없음 |
| 11 | `/healthz` 없음, 구조화된 에러 로그 없음 | `/api/health`만 있음 |

### P2 — 제품이 덜 된 부분

| # | 문제 |
|---|---|
| 12 | **첫 실행 경험이 없다.** 가입하면 곧장 미션 목록. 왜 해야 하는지, 하루 목표가 뭔지 알려주지 않음 |
| 13 | **학습 경로가 없다.** 시 19편이 평평한 목록. "어디까지 왔는지"가 안 보임 |
| 14 | **레슨 개념이 없다.** 미션 3개가 서로 이어지지 않고, 즉각 피드백·효과음·완료 연출이 없음 |
| 15 | 랭킹이 순위표뿐 — 리그(승급/유지)도, "나의 성장"도 없음 |
| 16 | 봇 문구가 5종 고정 — 첫 완료/복귀/랭킹 상승 같은 상황 구분이 없음 |
| 17 | 라이트 모드 없음(다크 전용), 디자인 토큰이 색에만 있고 간격·타이포는 하드코딩 |
| 18 | 아이콘이 전부 이모지 — OS마다 다르게 보이고 통일감이 없음 |
| 19 | 오프라인 시 앱 껍데기만 열리고 "오늘의 시"는 안 열림(API 캐시 안 함) |
| 20 | 접근성 미검증 — 대비, 스크린리더 라벨, 큰 글씨 설정 |

### P3 — 콘텐츠 정확성 (가장 조심할 부분)

| # | 문제 |
|---|---|
| 21 | **시 19편 본문이 정본과 대조되지 않았습니다.** `verified` 플래그 자리는 만들어 뒀지만 아무것도 `true`가 아니고, 미대조 상태가 화면에 표시되지도 않습니다 |
| 22 | 퀴즈 15문항의 사실 정보(출생지, 체포 혐의, 시집 간행 연도 등)는 **제가 쓴 것이고 출처가 달려 있지 않습니다.** 널리 알려진 내용이지만 검증 절차를 거치지 않았습니다 |
| 23 | 시 해설(`note`)도 제가 쓴 글입니다. 해석은 의견이라 "사실"과 구분해 표시해야 합니다 |

> **이 부분이 이번 작업에서 제일 위험합니다.** Phase 3의 설계 원칙을 §5에 따로 적었습니다.

---

## 3. Phase별 계획

각 Phase 끝에 **테스트 통과 → 브라우저 스크린샷 → 커밋 → 보고**를 하고 멈춥니다.

### Phase 0 — 디자인 시안 (서버 없이 열리는 단일 HTML)

- `docs/prototype.html` 1개 파일. 열면 바로 도는 정적 시안.
- 담을 화면: **별자리 홈 / 레슨 4단계(낭독→퀴즈→감상→완료) / 원고지 시 화면 / 온보딩 3장 / 동주 이야기 타임라인 / TMI 카드 / 스트릭·리그 / 다크·라이트 전환**
- 상태는 `localStorage`. 폰트는 시스템 폴백(시안 단계에서는 자체 호스팅 폰트를 끌어오지 않음).
- 데이터는 예시값이며 화면 안에 "시안용 예시"라고 표시합니다.
- **공개 방법**: `docs/` 폴더 + GitHub Pages(Settings → Pages → Source: `main` 브랜치 `/docs`).
  `gh-pages` 브랜치 대신 `/docs`를 권합니다 — 브랜치를 하나 덜 관리하고, 시안이 코드와 같은 커밋에 남습니다.
- **완료 기준**: 제가 폰(360px)·데스크톱 스크린샷으로 확인 → 링크와 이미지로 보고 → **확정 승인**을 받고 다음으로.

### Phase 1 — 디자인 전면 교체

1. **P0 3건 먼저 수정** (테스트 `.env` 격리, join 레이트리밋, 보안 헤더).
2. 디자인 토큰 정리: 색·간격·타이포·반경·그림자를 CSS 변수로. 라이트/다크 두 벌.
   - 밤하늘 남색 / 한지 미색 / 별빛 노랑 1색. 그라데이션은 **배경 한 곳에만**.
3. 폰트 자체 호스팅 (§6-B 결정 필요).
4. **커스텀 SVG 아이콘 세트** — 별, 펜촉, 책, 불꽃, 트로피, 사람, 방, 종 등 약 20종.
   선 굵기 1.75px·둥근 끝·24px 그리드로 통일. 인라인 `<svg>` 스프라이트 1개 파일.
5. **앱 아이콘/파비콘/PWA(maskable)/스플래시** 재제작 — 별 하나 + 펜촉.
6. **마스코트** "별이" 1종, 표정 4가지(기본·기뻐함·응원·잠듦). 로딩 화면(Phase 5)에서도 씁니다.
   - 실존 인물(윤동주)의 얼굴을 캐릭터로 만들지 않습니다. 사진도 쓰지 않습니다.
7. 시 화면을 원고지 톤으로 재조판 — 행간·연 간격·좌우 여백을 시 형식(verse/prose)별로.
8. 접근성: 대비 4.5:1 검사, 터치 44px, `aria-label`, `prefers-reduced-motion`, 큰 글씨 설정.
- **완료 기준**: 46개 테스트 통과 + 전 화면 스크린샷(다크·라이트, 360px) + 안티-AI 체크리스트 자가 점검표.

### Phase 2 — 듀오링고식 학습 구조

- **데이터 모델 추가**: `lessons`(시 1편 = 노드), `lesson_steps`, `user_lesson_progress`,
  `daily_goals`, `streak_freezes`, `leagues`, `league_members`.
  기존 `daily_missions`/`mission_completions`는 **유지**하고 그 위에 레슨을 얹습니다(기존 테스트 보존).
- **별자리 경로**: 시 19편을 주제별 5개 별자리로 묶고, 완료한 별에 불이 켜지며 선으로 이어집니다.
- **레슨 흐름**: 낭독 → 퀴즈(객관식 / 빈칸 / 순서 맞추기) → 감상 한 줄 → 완료 보상(별 획득 연출).
  문제마다 즉각 피드백 + 짧은 해설. 효과음·진동은 **기본 켜짐, 설정에서 끔**.
- **하트/목숨 없음** (요청대로). 틀리면 해설 보여주고 다시 풀게 합니다.
- **하루 목표**: 가볍게(1레슨) / 보통(2) / 열심히(3). 온보딩에서 선택, 나중에 변경 가능.
- **스트릭 보호권**: 별 포인트로 구매, 최대 2개 보유, 하루 빠지면 자동 소모.
- **주간 리그**: 방 단위 + 전체. 월요일 00시 정산 → 상위 승급 / 하위 유지(강등 없음 — 교육용).
  같이 보여줄 것: "이번 주 나의 성장"(지난주 대비 레슨 수·연속일·새 시).
- **온보딩 3~4장**: 닉네임 → 하루 목표 → 알림 시간. 알림 권한은 **맥락 설명 후** 요청.
- **봇 문구 재설계**: 상황 9종(첫 완료 / 3·7·30일 / 랭킹 상승 / 복귀 / 목표 달성 / 별자리 완성 / 응원 받음)
  × 변주 3~5개. 말투 규칙을 `content/voice.md`에 문서화.
- **완료 기준**: 기존 46개 + 신규 테스트 통과, 레슨 전 흐름 스크린샷.

### Phase 3 — 역사·스토리·TMI (정확성 최우선)

§5에 원칙을 따로 적었습니다. 요약:
- `content/timeline.js`, `content/tmi.js` 신설. 모든 항목에 `source_name`, `source_url`, `license`, `verified`, `last_checked`.
- **`verified: false`면 API 응답에서 제외** — 화면에 도달하지 않습니다(테스트로 강제).
- 서버에서만 외부 호출 → `external_cache` 테이블에 캐시(TTL) → 실패 시 큐레이션 폴백.
- 모든 카드 하단에 출처명·링크·라이선스 표기.
- **시 본문**: `verified` 플래그를 관리자 화면에 노출. 대조 절차를 README에 정리.
- **완료 기준**: `verified:false` 항목이 응답에 섞이지 않는다는 테스트 + 출처 표기 스크린샷.

### Phase 4 — 실사용 리스크 제거

- 보안: Origin 기반 CSRF 검증, 보안 헤더(CSP 포함), 입력 검증 전수 점검, 레이트리밋 확대,
  푸시 구독 정보 접근 제한, 비밀키 환경변수 강제(운영에서 미설정 시 기동 실패).
- 미성년자 관점: 수집 항목 최소화(닉네임·PIN만), 개인정보 처리 안내 화면, **계정·데이터 삭제**(즉시 삭제 + 되돌릴 수 없음 안내).
- 채팅 운영: 욕설 필터(한국어 변형 대응), 신고, 차단, 방장 강제 퇴장, 도배 제한(연속 메시지·초당 제한).
- **교사용 화면**: 방별 참여율 / 미완료 학생 / 이번 주 진도. **개인 순위는 띄우지 않습니다** — 참여 현황 중심.
- 안정성: `/healthz`, 구조화 로그, 푸시 실패 재시도(지수 백오프), 마이그레이션 점검,
  백업 스크립트(`VACUUM INTO`), 복원 스크립트.
- 오프라인: 서비스워커가 **오늘의 시·레슨 데이터를 캐시** → 비행기 모드에서도 열림.
  채팅·랭킹은 온라인 필요 → "지금은 오프라인입니다" 안내 띄우기.
- 성능: Lighthouse 모바일 Performance/Accessibility/PWA **90점 이상** 목표. 미달 시 실제 점수를 그대로 보고합니다.

### Phase 5 — GitHub + 클라우드 배포

- 저장소 정리: `.gitignore` 보강, `.env.example`(값 없이 키 이름만), `LICENSE`(코드: MIT 제안),
  `docs/LICENSES.md`(폰트·아이콘·외부 콘텐츠 라이선스).
- **커밋 전 비밀 검사**: 스테이지된 파일에서 VAPID 키·세션 시크릿·`.db`·`.env` 패턴을 찾아 보고합니다.
- 환경변수 전면화: `PORT` `NODE_ENV` `SESSION_SECRET` `VAPID_*` **`DATABASE_PATH`**(기존 `DB_PATH`도 계속 인식)
  `APP_TIMEZONE` `ADMIN_HANDLES` `INTERNAL_TICK_TOKEN` `PUBLIC_URL`.
- Node 버전 고정: `engines`, `.node-version`, `render.yaml` 삼중 일치.
- `render.yaml` + 수동 설정 안내. 헬스체크 `/healthz`.
- **DB 영속성**과 **스케줄러**는 §6-C, §6-D에서 선택지를 드립니다. 가장 중요한 결정 두 개입니다.
- 잠든 서버 깨어나는 동안 보여줄 로딩 화면(마스코트 "별이" 잠듦 표정).
- 배포 후 실제 URL에서 가입→온보딩→레슨→채팅→랭킹→**푸시 수신**까지 확인.
- 학생용 설치 안내 1장(`docs/설치안내.md`, 스크린샷 포함) — "서버와 연결되어 동작하며, 오프라인에서는 오늘의 시·레슨만 열립니다" 명시.
- CI: GitHub Actions — push마다 테스트, `main` 병합 시 자동 배포.
- 운영 문서: 로그 보기 / 백업·복원 / 시 본문 정정 / `verified` 처리.

### Phase 6 — 마무리

- 신규 기능 테스트 + Playwright로 핵심 흐름 스크린샷 검증.
- README 전면 갱신, 남은 리스크와 다음 후보 정리.

---

## 4. 안티-AI 체크리스트 (Phase 1·6에서 자가 점검)

- [ ] 보라→파랑 그라데이션을 배경 1곳 외에 쓰지 않았는가
- [ ] 모든 요소가 똑같은 반경·그림자의 카드가 아닌가 (구획마다 다른 처리)
- [ ] 글래스모피즘(blur)을 상단바 1곳 외에 쓰지 않았는가
- [ ] UI 이모지가 0개인가 (커스텀 SVG로 전부 교체)
- [ ] 의미 없는 등장 애니메이션이 없는가 (별 켜짐·완료 연출에만)
- [ ] 빈 화면이 회색 placeholder가 아니라 다음 행동을 제안하는가
- [ ] 문구가 "~하세요!" 남발이 아닌가 (담백한 톤 유지)

---

## 5. 사실 정확성 원칙 (Phase 3에서 반드시 지킬 것)

**제가 아는 것을 사실처럼 쓰지 않겠습니다.** 구체적으로:

1. **`verified`의 의미를 좁게 정의합니다.** `verified: true`는 "그럴듯하다"가 아니라
   **"내가 `source_url`을 실제로 가져와 그 본문에서 해당 진술을 확인했다"**는 뜻으로만 씁니다.
   확인 스크립트(`npm run verify:content`)가 출처를 다시 받아 대조하고 결과를 보고합니다.
2. **`verified: false`는 화면에 나오지 않습니다.** 서비스 계층에서 걸러내고, 테스트로 강제합니다.
3. **의견과 사실을 나눕니다.** 시 해설(`note`)은 `type: 'reading'`(해석)으로, 연대기·TMI는
   `type: 'fact'`(사실)로 태그하고 화면 표기도 다르게 합니다. 해설에는 "이 글은 이 앱이 쓴 감상 안내입니다" 표시.
4. **출처 후보**: 한국민족문화대백과사전, 국가유산청, 연세대·윤동주문학관 등 공식 자료, 한국어 위키백과(CC BY-SA 4.0 — 출처·라이선스 표기).
   접근이 막히면 해당 항목은 **넣지 않습니다**(추측으로 메우지 않습니다).
5. **기존 퀴즈 15문항도 소급 검증합니다.** 검증 못 한 문항은 `verified:false`로 내려 화면에서 빠집니다.
   현재 통과 중인 테스트가 "퀴즈가 존재한다"를 전제하므로, 검증 결과 문항이 줄면 그 사실을 그대로 보고하겠습니다.
6. **시 본문은 제가 검증할 수 없습니다.** 정음사 『하늘과 바람과 별과 詩』(1955 증보판),
   왕신영 외 『사진판 윤동주 자필 시고전집』은 온라인 원문이 없습니다.
   → 관리자 화면에 "미대조 19편"을 띄우고, 사람이 대조 후 체크하는 절차를 만듭니다. **제가 임의로 `true`로 바꾸지 않습니다.**

> 저작권 참고: 윤동주(1917~1945) 작품은 보호기간 만료로 공유 저작물입니다. 이건 확인된 사실입니다.
> 반면 **개별 시의 정확한 표기**(띄어쓰기·옛 표기·행 구분)는 판본마다 달라, 대조가 필요합니다.

---

## 6. 당신이 결정해야 할 것

### A. 의존성 추가 (승인 필요)

| 패키지 | 구분 | 이유 |
|---|---|---|
| `playwright` | **개발용만** | Phase마다 요구하신 브라우저 스크린샷 검증. 런타임에는 안 들어갑니다 |
| `lighthouse` | **개발용, 선택** | Phase 4의 90점 목표를 숫자로 확인. 무겁다면(≈80MB) 빼고 수동 점검으로 갑니다 |

**런타임 의존성은 `express` + `web-push` 그대로 둡니다.** CSRF·보안 헤더·욕설 필터·백업·외부 호출은
전부 내장 기능으로 직접 구현합니다(추가 패키지 없음).

### B. 폰트 — 실측 결과를 근거로

자체 호스팅을 위해 실제로 받아 보고 재 봤습니다.

| 후보 | 서브셋 파일 | 예상 용량 | 비고 |
|---|---|---|---|
| Gowun Batang 400·700 (시 본문) | 190개 | **약 3.3MB** | OFL. 원고지 톤에 잘 맞음 |
| Noto Sans KR 400·500·700 (UI) | 372개 | 약 6.7MB | OFL |
| IBM Plex Sans KR 400·500·700 | 282개 | 약 5.0MB | OFL |
| 시스템 폰트 스택 (UI) | 0개 | **0MB** | iOS=Apple SD Gothic Neo, Windows=맑은 고딕, Android=Noto |

- **Pretendard는 쓸 수 없습니다.** GitHub Releases(403)와 jsDelivr(연결 차단) 모두 이 환경에서 막혀 받을 수 없었습니다.
  꼭 원하시면 직접 받아 `public/fonts/`에 넣으시면 코드가 바로 인식하도록 만들어 두겠습니다.

**제 추천 (①)**: 시 본문 = **Gowun Batang 자체 호스팅**, UI = **시스템 폰트 스택**.
→ 저장소 +3.3MB, 폰이 자기 OS 폰트를 쓰니 가장 자연스럽고 빠르며, 오프라인에서도 확실합니다.
타이포의 승부처인 시 화면에만 힘을 줍니다.

**대안 (②)**: 시 본문 Gowun Batang + UI IBM Plex Sans KR 자체 호스팅 → +8.3MB, 어느 기기에서나 완전히 동일한 화면.

**① / ② 중 무엇으로 할까요?**

### C. DB 영속성 — 가장 중요한 결정

재배포하면 파일이 사라지는 서비스가 많습니다. 학급 데이터가 통째로 날아가는 문제입니다.

| 방식 | 데이터 안전 | 비용 | 작업량 | 비고 |
|---|---|---|---|---|
| **① Render + 영구 디스크** | 높음 | **유료 구간일 가능성 큼** | 작음 | `DATABASE_PATH=/var/data/app.db`. 권장 |
| ② 무료 플랜 + 정기 백업 내보내기 | 낮음 | 무료 | 중간 | 재배포·재시작 때 유실. 백업 사이 기록은 사라짐. **교실 운영에는 권하지 않습니다** |
| ③ 외부 DB(Turso/libSQL 또는 Postgres)로 이전 | 높음 | 무료 구간 있음 | **큼** | 데이터 접근 계층 분리 필요. `node:sqlite` 제약에서 벗어남 |

**제 추천**: **①로 가되, 코드를 ③으로 옮기기 쉽게** 짜 둡니다.
구체적으로 Phase 4에서 SQL 호출을 `services/`에 가두고 `db.js`에 얇은 어댑터를 두어,
나중에 ③이 필요해지면 `db.js`만 바꾸면 되게 합니다. 지금 당장 ③을 하는 건 과합니다.

> ⚠️ **직접 확인하셔야 할 항목**: Render의 영구 디스크가 어느 요금제부터인지, 무료 플랜 조건이 어떤지는
> 자주 바뀝니다. 제가 여기 적는 순간 틀릴 수 있어 단정하지 않겠습니다. 배포 직전에 Render 요금제 문서를 확인해 주세요.

**①/②/③ 중 무엇으로 할까요?**

### D. 스케줄러 — 서버가 잠들면 알림이 안 갑니다

| 방식 | 장점 | 단점 |
|---|---|---|
| **① 외부 cron → `/internal/tick`** | 무료·유료 어디서나 동작. 서버가 자도 cron이 깨움. 재시작에도 안전 | 외부 서비스 1개 의존, 토큰 관리 필요 |
| ② 상시 실행(유료) + 내부 타이머 | 구조가 단순 | 요금 발생. 배포 중 재시작 시각에 걸리면 그 알림은 유실 |

**제 추천**: **①**. 다만 두 방식이 **같은 함수**를 부르게 만들어 둘 다 켜도 안전하게 합니다.
핵심은 **멱등성**입니다 — 지금 `tick()`은 "정각이 아니면 건너뜀"이라 밀린 일을 못 따라잡으므로,
**"마지막 처리 시각 이후 밀린 작업을 모두 실행하되, `push_log`와 메시지 중복 검사로 두 번 보내지 않는다"**로 바꿉니다.
그래서 cron이 5분 늦게 불러도, 두 번 불러도 결과가 같습니다.

**①/② 중 무엇으로 할까요?**

### E. 배포처

요청하신 대로 **Render** 기준으로 `render.yaml`을 씁니다. Fly.io·Railway를 원하시면 말씀해 주세요.
(웹푸시 때문에 HTTPS는 필수이고, 세 곳 모두 기본 제공합니다.)

### F. Phase 0 시안 공개

`main` 브랜치 `/docs` + GitHub Pages를 권합니다. 설정은 제가 파일을 만들고,
**GitHub 웹에서 Settings → Pages → Source 지정**은 저장소 소유자인 우빈님이 한 번 눌러 주셔야 합니다(제 권한 밖).

### G. 관리자 권한

교사용·관리자 화면 접근을 어떻게 정할까요?
**추천**: 환경변수 `ADMIN_HANDLES=동주,선생님아이디` (쉼표 구분). 별도 계정 체계 없이 가장 단순합니다.
방장은 자기 방에 한해 교사용 화면을 볼 수 있게 합니다.

---

## 7. 범위 밖 (이번에 안 하는 것)

- 카카오톡·디스코드 연동 — `postBotMessage()` 확장 지점만 유지하고 실제 연동은 하지 않습니다(심사·키 필요).
- 음성 녹음 낭독 제출 — 저장 용량과 개인정보 문제가 커서 별도 논의가 필요합니다.
- 초등용 난이도 묶음 — 구조는 열어 두되 콘텐츠 제작은 다음 차례로.
- 시 본문 정본 대조 — 제가 할 수 없습니다(§5-6). 절차와 화면만 만듭니다.

---

## 8. 진행 방식 확인

1. 이 계획서를 검토하시고, **§6의 A~G를 정해 주세요**(전부 "추천대로"라고 하셔도 됩니다).
2. "진행"이라고 하시면 **Phase 0(시안)**부터 시작합니다.
3. Phase마다 **바뀐 것 / 스크린샷 / 남은 문제 / 결정 필요 사항**을 보고하고 멈춥니다.
4. 추측으로 채운 사실 정보가 생기면 **그 자리에서 따로 표시**하겠습니다.
